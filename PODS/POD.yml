apiVersion: v1
kind: Pod
metadata:
  name: angular-pod
  labels:
    app: web
spec:
  containers:
    - name: web-ctrangular
      image: manishadotnet/k8spratice:angularappimg
      ports:
        - containerPort: 8084